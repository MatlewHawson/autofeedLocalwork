This is the folder where libraries used in Arduino sketches can be found. You don't need to get all of them,
just the libraries that are specified as not being native to the IDE (the sketches themselves are where you can find
this out).
